# ---------------------------------------------------------------
# LOCAL / TAURI BUILDS ONLY (VITE_ prefix = bundled into client)
# Do not set these on Vercel — use the server-side vars below instead
# ---------------------------------------------------------------
VITE_SAZED_URL=http://localhost:8000
VITE_API_KEY=

# Google OAuth — public client ID is safe to expose
VITE_GOOGLE_CLIENT_ID=
# Only this email is allowed through the auth gate
VITE_ALLOWED_EMAIL=you@gmail.com

# ---------------------------------------------------------------
# VERCEL / SERVER-SIDE ONLY (no VITE_ prefix = never in the bundle)
# Set these in the Vercel dashboard, not here
# ---------------------------------------------------------------
SAZED_URL=
SAZED_API_KEY=
ALLOWED_EMAIL=you@gmail.com
